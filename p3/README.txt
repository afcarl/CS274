Name: Rory MacQueen
SUNETID: macqueen

Run my program as:

python md_macqueen.py --if 1FW4_med.rvc

Run it with --help to see the available options

The .euc files were created by running the script and calling the print_atom_dist() function at the end of the simulation in order to print out the distances between the two atoms in question.

The graphs were created using an r-plot
